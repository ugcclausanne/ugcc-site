{% set _current = (page_key or (page and page.fileSlug)) %}
{% if _current == 'news' %}
<section class="section-list">
  <div class="container">
    <div class="grid cols-3">
      {% for item in sections.news %}
        {% include "partials/card-article.njk" %}
      {% endfor %}
    </div>
  </div>
</section>
{% endif %}
